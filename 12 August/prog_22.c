#include <stdio.h>

/* 
    Add your documentation here:
    - Missing: Title Adding the Number 
    - Missing: Author Mayank Balyan 
    - Missing: Description Program used for adding the two numbers input in the same line

    - Missing: Date 12 August
*/

int main() {
    int a, b, sum;

    // Ask the user to enter two numbers
    printf("Enter two integers: ");
    scanf("%d %d", &a, &b);

    // Missing: describe this instruction
//adding the two numbers that are taken in the form of input
    sum = a + b;

    // Display the result
    printf("Sum = %d\n", sum);

    return 0;
}

